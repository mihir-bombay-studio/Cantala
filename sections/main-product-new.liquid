
<style>
  .breadcrums {
  	margin-bottom: 66px;
  }
  .breadcrum {
    font-size: 13px;
    margin-right: 18px;
    position: relative;
    opacity: .5;
  }  
  .breadcrum:last-child {  	
    margin-right: 0;    
    opacity: 1;
  }
  
  .breadcrum::after {
  	content: "/";
    position: absolute;
    right: -12px;
  }
  
  .breadcrum:last-child::after { 
  	content: "";
  }
</style>

<style>
  .product-row-1, .product-row-2 {
  width: calc( 50% - 54px );
  }
  .slider-for {
    margin-bottom: 16px;
}
  .slider-for .slick-track {
    display: flex;
}
  .slider-for img {
    align-self: center;
    border-radius: 4px;
    height: 500px;
    object-fit: contain;
}
  .slider-nav .slick-track {
    margin-left: 0;
}
  .slider-nav img.slick-slide.slick-current {
    box-shadow: inset 0 0 0 2pt #1e2d51!important;
    padding: 2px;
    border-radius: 4px;
}
  .slider-nav img {
    margin-right: calc( (768px - (81px*8)) / 7 );
}
  .slider-nav img {
    margin-bottom: 8px;
  }
  h1.product-title {
    font-size: 46px;
    color: #342D25;
    font-family: 'Athelas',serif;
  }
  .product-price {
    margin-top: 15px;
    color: #342D25;
    font-weight: 500;
    font-size: 22px;
    opacity: .7;
    margin-bottom: 44px;
    display: block;
  }
  
  .accordion-description {
    cursor: pointer;
    transition: 0.4s;
    position: relative;
    margin-top: 0;
    margin-bottom: 0;
  }

  .panel {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.2s ease-out;
  }
  .accordion-description svg {
    width: 18px;
    height: auto;
    position: absolute;
    right: 0;
    top: calc( 50% - 9.5px );
    transition: .2s ease;
  }
  .accordion-description .symbol-minus svg {
    top: calc( 50% - 13px );
  }
  
  .accordion-description .symbol-minus svg {
    opacity: 0;
  }
  .accordion-description.accordion-active .symbol-plus svg {
    opacity: 0;
  }
  
  .accordion-description.accordion-active .symbol-minus svg {
    opacity: 1;
  }
  hr.product-seperator {
    margin: 24px 0 44px;
  }
  ul.checklist {
    margin: 0;
    list-style: none;
    list-style-position: inside;
}
  ul.checklist {
    padding: 40px 0 0 0;
}
  ul.checklist li {
    padding: 0 0 21px 52px;
    position: relative;
    color: #342D25;
    font-weight: 500;
}
  ul.checklist li:before {
    position: absolute;
    top: 0;
    left: 0;
    content: ' ';
    width: 25px;
    height: 25px;
    background-image: url({{ "tick.svg" | asset_url }});
    background-size: contain;
    background-repeat: no-repeat;
}
  
  .qty-wrapper {
    display: flex;
    align-items: center;
    border: 1.5px solid #342D25;
    padding: 16px;
    width: 100%;
    justify-content: space-between;
  }
  .qty-btn.symbol-minus {
    opacity: .5;
  }
  .qty-number {}

  .qty-btn {
    height: 8px;
    width: 8px;
    cursor: pointer;
  }
  .qty-btn svg {
    width: 100%;
    height: 100%;
  }
  .qty-container {
    display: flex;
    width: 58px;
    justify-content: space-between;
  }
  .m-row.grouped:first-child {
    width: 46%;
    padding-right:6px;
  }
  .m-row.grouped:last-child {
    width: 54%;
    padding-left:6px;
  }
  span#AddToCart {
    margin-top: 0;
    width: auto;
    padding: 17.5px 18px;
    font-family: var(--font-body-family);
    font-weight: 400;
    border: 0;
    letter-spacing: .8px;
    font-size: 16px;
    background-color: #342D25;
    color: #F4EDE2;
    text-align: center;
}
</style>

<div class="container">
  <div class="breadcrums">
    <a href="/" class="breadcrum">Home</a>
    <a class="breadcrum active">{{ product.title | escape }}</a>
  </div>
  
  
  <div class="container product padd-top-m flex-box justify-between top-puzzle-left">
    <div class="product-row-1">
      <div class="slider-for">
        {% for media in product.media %}
        {% case media.media_type %}
        {% when 'image' %}
        <img src="{{ media | img_url: 'x600' }}" alt="{{ media.alt | escape }}" {% unless forloop.first %}loading="lazy"{% endunless %}>
        {% when 'video' %}
        <video playsinline="playsinline" preload="auto" autoplay="autoplay" muted="muted" loop="loop">
          {% for video in media.sources %}{% if video.format == "mp4" %}<source src="{{ video.url }}" type="video/mp4">{% endif %}{% endfor %}
        </video>
        {% else %}
        <img src="{{ media | img_url: 'x600' }}" alt="{{ media.alt | escape }}" {% unless forloop.first %}loading="lazy"{% endunless %}>
        {% endcase %}          
        {% endfor %}
      </div>
      <div class="slider-nav{% for media in product.media %}{% if forloop.length < 8 %} no-transform{% endif %}{% endfor %}">
        {% for media in product.media %}          
        {% case media.media_type %}
        {% when 'image' %}
        <img src="{{ media.preview_image | img_url: '200x' }}" alt="{{ media.alt | escape }}"> 
        {% when 'video' %}
        <div class="video-btn"><img src="{{ media.preview_image | img_url: '200x' }}" alt="{{ media.alt | escape }}"> </div>
        {% else %}
        <img src="{{ media.preview_image | img_url: '200x' }}" alt="{{ media.alt | escape }}"> 
        {% endcase %} 
        {% endfor %}
      </div>
      
      <ul class="checklist">
        <li>Voor 12u besteld - morgen geleverd*</li>
        <li>Focus op duurzaamheid</li>
        <li>Grootste assortiment in premium koffie & thee in Benelux</li>
        <li>Gratis verzending vanaf €30 {% include "icon-belgium" %} & €45 {% include "icon-netherlands" %}</li>
        <li>Beleef met cantata je eigen wow-effect</li>

      </ul>
      
    </div>
    <div class="product-row-2">
      <div class="product-top">
        <div class="product-row sub-marg-bottom-xs">
          <div class="product-col">
            <h1 class="product-title">{{ product.title | escape  }}</h1>
          </div>
          <div class="product-col">
            <span class="product-price">{{ product.price | money  }}</span>
          </div>
        </div>
        
        
        
        
        
        
        
        {%- unless product.has_only_default_variant -%}
        <div class="variant-options-wrapper">
          <variant-radios class="no-js-hidden">
            <div class="fieldset-wrapper style-scrollbar">
              {%- for option in product.options_with_values -%}
              	{%- for value in option.values -%}
              		<div class="box-selector {% if option.selected_value == value %}selected{% endif %}">{{option.name}}{{ value | escape }}</div>
              	{%- endfor -%}
              {%- endfor -%}
            </div>
            <script id="product-json" type="application/json">
              {{ product.variants | json }}
            </script>
          </variant-radios>
        </div>
        {%- endunless -%}
        
                
      </div>

      <div class="product-details">
        <div class="flex-box">
          <div class="m-row grouped">
            <div class="qty-wrapper" data-quantity="1">
              <span class="qty-name">Aantal:</span>
              <span class="qty-container">
                <span class="qty-btn symbol-minus">{% include "icon-minus" %}</span>
                <span class="qty-number">1</span>
                <span class="qty-btn symbol-plus">{% include "icon-plus" %}</span>
              </span>
            </div>
          </div>

          <div class="m-row grouped">
            <span class="addcart btn" id="AddToCart" data-value="{{ product.selected_or_first_available_variant.id }}">Voeg toe aan winkelwagen</span> 
          </div>
        </div>
      </div>

      <div class="accordion-description">  
        <p class="rel">Beschrijving <span class="symbol symbol-plus">{% include "icon-plus" %}</span></p><span class="symbol symbol-minus">{% include "icon-minus" %}</span></p>
        <hr class="product-seperator">
      </div>
      <div class="panel extra-panel">
        {{ product.description }}
      </div>

    </div>
  </div>
  
</div>


<script>
  
  
  $(".qty-wrapper .symbol-plus").click(function(){
      var adder = 1;
      $(this).parent().parent().data("quantity",parseInt($(this).parent().parent().data("quantity"))+1);
      $(this).parent().find('.qty-number').text($(this).parent().parent().data("quantity"));   
      if($(this).parent().parent().data("quantity") < 2) {
        $(this).parent().find('.symbol-minus').css('opacity','0.5');
      } else {
        $(this).parent().find('.symbol-minus').css('opacity','1');
      }
      //$(".product-price").text("€"+cartBtnPrice());       
    }); 
    $(".qty-wrapper .symbol-minus").click(function(){
      if($(this).parent().parent().data("quantity") > 1) {
      	var adder = 1;
        $(this).parent().parent().data("quantity",parseInt($(this).parent().parent().data("quantity"))-1);
        $(this).parent().find('.qty-number').text($(this).parent().parent().data("quantity")); 
        if($(this).parent().parent().data("quantity") < 2) {
        	$(this).css('opacity','0.5');
        } else {
        	$(this).css('opacity','1');
        }
      //$(".product-price").text("€"+cartBtnPrice());
      }
    });   
  
  //faq script
  var acc = document.getElementsByClassName("accordion-description");
  var i;

  for (i = 0; i < acc.length; i++) {
    acc[i].addEventListener("click", function() {
      this.classList.toggle("accordion-active");
      var panel = this.nextElementSibling;
      if (panel.style.maxHeight) {
        panel.style.maxHeight = null;
      } else {
        panel.style.maxHeight = panel.scrollHeight + "px";
      }
    });
  }
</script>

{% schema %}
  {
    "name": "Product section",
    "settings": []
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
